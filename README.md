How to use:
1. Run install_with_python.bat
2. Run Calibration.py
3. Run run.bat

I added move the user input and questions into calibration and made it generate a config folder. This makes it so the user only needs to calibrate once. 
